# Data Structures

```@meta
CurrentModule = MichiBoost
```

## Pool

```@docs
Pool
slice
```

The [`Pool`](@ref) constructor accepts a `text_features` keyword argument (same format as `cat_features`) that is treated identically as categorical features.
